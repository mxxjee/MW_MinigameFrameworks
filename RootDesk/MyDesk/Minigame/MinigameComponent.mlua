@Component
script MinigameComponent extends Component

	method void InitializePractice(MinigameData gameData)
		-- 연습 모드 미니게임 초기화를 수행한다.
	end

	method void InitializeMatched(MinigameData gameData, GameMatchInfo matchInfo)
		-- 매칭 모드 미니게임 초기화를 수행한다.
	end

	method void Update(number deltaTime)
		-- 진행 중인 미니게임의 프레임 갱신을 처리한다.
	end

	method void Release()
		-- 미니게임 종료 시 필요한 정리를 수행한다.
	end

end
